<%= form_for(@outfit) do |f| %>
<p>Outfit Name:</p>
    <%= f.text_field :name %><br /><br />
    <p>Rating:</p>
    <%= f.number_field :rating %><br /><br />
<%= f.fields_for :clothing_outfits do |co| %>
  <p>Your Tops:</p>
    <%= co.collection_check_boxes :piece_id, @pieces, :id, :name %>
  <%= co.submit class: "btn btn-primary"%>
  <br /><br />
  <% end %>
<% end %>


